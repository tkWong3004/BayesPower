% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BF_users_functions.r
\name{BF10.t.test.two.sample}
\alias{BF10.t.test.two.sample}
\title{Bayes Factor for Two-Sample Bayesian t-Test}
\usage{
BF10.t.test.two.sample(
  tval,
  N1,
  N2,
  model,
  location,
  scale,
  dff,
  hypothesis,
  e = NULL
)
}
\arguments{
\item{tval}{Numeric scalar. Observed t-value from the two-sample t-test.}

\item{N1}{Numeric scalar. Sample size of group 1 (must be > 2, will be rounded to nearest integer).}

\item{N2}{Numeric scalar. Sample size of group 2 (must be > 2, will be rounded to nearest integer).}

\item{model}{Character. Analysis prior under the alternative hypothesis:
\code{"normal"}, \code{"moment"} (normal-moment prior), or \code{"t"}.}

\item{location}{Numeric scalar. Location parameter of the analysis prior.}

\item{scale}{Numeric scalar > 0. Scale parameter of the analysis prior.}

\item{dff}{Numeric scalar. Degrees of freedom for the analysis prior (required if model = \code{"t"}; ignored otherwise).}

\item{hypothesis}{Character. Hypothesis to test: two-sided (\code{"!="}), right-sided (\code{">"}), or left-sided (\code{"<"}).}

\item{e}{Optional numeric. Bounds for an interval null:
\itemize{
\item For \code{hypothesis = "!="}, must be a numeric vector of length 2 with distinct finite values.
\item For \code{">"}, must be a single numeric scalar > 0.
\item For \code{"<"}, must be a single numeric scalar < 0.
}}
}
\value{
A list of class \code{BFvalue_t} containing:
\describe{
\item{type}{Character string describing the test type.}
\item{bf10}{Computed Bayes factor BF10.}
\item{tval}{Observed t-value.}
\item{df}{Degrees of freedom (currently NA / not computed).}
\item{analysis_h1}{List of prior model parameters used for the alternative hypothesis.}
\item{hypothesis}{Hypothesis tested (\code{"!="}, \code{">"}, or \code{"<"}).}
\item{e}{Interval bounds used, if any.}
\item{N1}{Sample size of group 1 (rounded integer).}
\item{N2}{Sample size of group 2 (rounded integer).}
}
}
\description{
Compute the Bayes factor (BF10) for a two-sample independent-samples t-test
under a specified prior model. Supports both point-NaN and interval-NaN hypotheses.
}
\examples{
BF10.t.test.two.sample(
  tval = 2.1,
  N1 = 30,
  N2 = 30,
  model = "t",
  location = 0,
  scale = 0.707,
  dff = 1,
  hypothesis = "!="
)

# Using an interval null for a right-sided hypothesis
BF10.t.test.two.sample(
  tval = 1.8,
  N1 = 25,
  N2 = 25,
  model = "normal",
  location = 0,
  scale = 1,
  dff = 0,
  hypothesis = ">",
  e = 0.2
)

}
